apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: coffee-dev
resources:
- ../../base

patches:
- path: deployment.yaml
  target:
    kind: Deployment
    name: cart

images:
- name: cart
  newName: cart
  newTag: latest
